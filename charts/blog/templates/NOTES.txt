{{ .Chart.Name }} {{ .Chart.Version }} deployed as {{ include "blog.fullname" . }}.

Image:
  {{ include "blog.image" . }}
{{- if not .Values.image.digest }}

  WARNING: image.digest is empty, so this release is pinned to a mutable tag.
  Releases published by .github/workflows/release.yml always set it.
{{- end }}
{{- if .Values.ingress.enabled }}

URL:
  https://{{ include "blog.primaryHost" . }}
{{- else }}

No Ingress. Reach it with:
  kubectl -n {{ .Release.Namespace }} port-forward svc/{{ include "blog.fullname" . }} 8080:{{ .Values.service.port }}
{{- end }}

Verify the rollout:
  kubectl -n {{ .Release.Namespace }} rollout status deploy/{{ include "blog.fullname" . }}
  helm test {{ .Release.Name }} -n {{ .Release.Namespace }}
